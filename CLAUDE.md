@AGENTS.md

# Project notes

- UI language is Korean (no i18n framework; strings are inline). Keep new UI text, validation messages and toasts in Korean. `html lang="ko"`, font is Pretendard via next/font/local.

- Internal LAN app: Next.js 16 App Router + SQLite (better-sqlite3 + Drizzle) + shadcn/ui. Light theme only; never add `.dark` styles or theme toggles.
- Auth is homegrown cookie sessions. `src/proxy.ts` does the cookie-only redirect; `requireUser()` in `src/lib/auth/dal.ts` does the DB-backed check. New pages go under `src/app/(app)/` so they are guarded. Only `/login` is public. The proxy must never redirect *away* from `/login` based on the cookie alone; that caused a redirect loop with stale cookies. The login page does the DB-backed check.
- Call `requireUser()` in every server action that touches data.
- DB access is synchronous (better-sqlite3): use `.get()` / `.all()` / `.run()`, not `await`.
- Schema changes: edit `src/lib/db/schema.ts`, then `pnpm db:generate`. Migrations auto-apply on app startup.
- Use `pnpm`. `pnpm db:seed` creates the admin user from `.env`.
- Team member records live in `members` (separate from login `users`). Mutations go through server actions in `src/lib/members/actions.ts`, which call `requireAdmin()`. Validation is zod in `src/lib/members/schema.ts`.
- Dialog forms mount their inner form only while open so `useActionState` resets each time (see `member-dialog.tsx`).
- `NativeSelect` from `components/ui/native-select` *is* the `<select>`; pass `id/name/defaultValue` to it directly and put `NativeSelectOption` children inside. Never nest a raw `<select>` in it (renders invisible).
- If a server action's success swaps the client form for a different component in the same commit, handle success inside the action wrapper (see `withDone` in `users/account-dialog.tsx`), not in a `useEffect` of the form.
- SQLite FK caveat: drizzle-kit drops `ON DELETE …` when generating `ALTER TABLE … ADD COLUMN … REFERENCES`. Check generated SQL for FK columns added to existing tables; fix with a `drizzle-kit generate --custom` table rebuild (see `drizzle/0003_*`). The `sqlite3` CLI has foreign keys OFF by default: prefix manual deletes with `PRAGMA foreign_keys=ON;`.
- Dates: all keys are `YYYY-MM-DD` strings in Asia/Seoul via `src/lib/dates.ts`; working-day logic lives in `src/lib/workdays.ts` (Mon–Fri minus `holidays` rows). Week key = Monday.
- Delete flows: server actions return `{ ok, error }` and never throw to the client; client wraps the call in try/catch and toasts.
- Team leader = `members.is_leader`, one per team, set via `setTeamLeader` (admin only). It is display-only for now; if leaders get permissions later, check `isLeader` + same `team` in the server action.
- `Tooltip` needs the `TooltipProvider` that lives in the root layout.
- Dashboard week view is a generic timeline: build `TimelineItem[]` (start/end keys clipped to the week, label, member colour from `memberColorMap`) and pass to `WeekTimeline`. Add team schedules by pushing more items in `week-leaves.tsx` (or a sibling), not by adding rows/columns.
- Member colours are per member (`MEMBER_PALETTE`, by id rank); leave *type* colours (`LEAVE_BADGE_CLASS`) are used on the monthly calendar and status badges. Keep that distinction unless the user asks to unify.
- Keep the dashboard minimal (daily forms + week timeline + buttons). New team-level views get their own page under `src/app/(app)/` plus a nav entry in `main-nav.tsx`, not another dashboard section.
- Milestones: Gantt in `src/app/(app)/milestones/gantt.tsx` is a server component; bar geometry is percentage of the visible day range. Detail is URL-driven (`?m=ID`) and server-rendered inside a thin client `DetailDialog`; server actions revalidate `/milestones` so the open dialog refreshes. Permissions live in `milestoneAccess()`; always call it in actions, never trust the client.
- Posting a milestone 현황 update is the normal way status/progress change; `updateMilestone` also logs an update row when status/progress differ.
- Demo data: `scripts/seed-mock.ts` (`pnpm db:seed:mock`, `--reset`, `--clean`). It identifies its own rows by the MOCK_* name lists, so keep those lists in sync if you add mock entities. Scripts must not import modules that import `server-only` (e.g. `workdays.ts`); reimplement small helpers instead.
- Daily plan = `daily_tasks` rows (title/status/note/position); `daily_logs.plan` is legacy and no longer written, `daily_logs.done` holds free-text extras. Team/logs pages render both via `components/task-lines.tsx`. Task actions check ownership through `requireMember()` + memberId match.
- Client components must not import runtime values from `src/lib/db/schema.ts` (type-only imports are fine). Enum constants (`TASK_STATUSES`, `MILESTONE_STATUSES`, `LEAVE_TYPES`) live in `lib/*/types.ts`; schema imports them, not the other way round. Importing the schema into client code pulls drizzle into the browser bundle and broke under HMR.
- Leader reviews: `daily_reviews` + `reviewerContext()` in `lib/reviews/queries.ts` (admin all; leader = own team, not self; member = own only). Always filter with `canSee` before rendering reviews and check `canReview` in the action. `daily_tasks.note` column is unused by the UI now (per-task memo was removed by request); leave it alone.
- IA (phase 1 of the restructure): 4 nav areas 내 업무 `/my/*`, 팀 `/team*`, 일정 `/schedule`, 관리 `/admin/*` (admin only; layout redirects others). Each area has a `SubNav` tab bar; page headings inside areas are `h2`. New pages go under the matching area and get a tab, not a top-level nav item. Old paths keep redirect stubs.
- Teams are rows in `teams` (`leader_member_id`). Never read `members.team` / `members.is_leader` (columns removed). Use `allMembers()`/`memberById()` (`Member` type from `lib/members/types.ts`, includes `team` + `isLeader`) and `milestonesInRange()`/`milestoneById()` (`MilestoneRow`). Team leader is set in 관리 → 팀.
- SQLite migration style used for 0008: `ADD COLUMN` + data `UPDATE` + `DROP COLUMN` (SQLite ≥ 3.35), never a table rebuild while foreign keys are on (implicit DELETE cascades). drizzle-kit `generate` needs a TTY for rename prompts; use `generate --custom` and write the SQL by hand, keeping the snapshot in sync with the schema file.
- Phase 2 (연결): `weekly_items` (status, milestoneId, monthlyGoalId, assignedBy) and `monthly_goals` (status, milestoneId); `daily_tasks.weekly_item_id` links a day item to a week item. Actions in `lib/plans/actions.ts` (owner-only via `requireMember`), queries in `lib/plans/queries.ts`. `pullWeeklyItemToDay` creates the linked daily task and bumps the week item to in_progress. Status enums reuse `TASK_STATUSES`.
- drizzle-kit: `generate --custom` does NOT refresh the snapshot. After hand-written migrations, run a real `generate` with prompts auto-answered (`printf '\r'` through `script`) and replace its SQL with `SELECT 1;` (see `drizzle/0010_snapshot_sync.sql`), then confirm `generate` reports "No schema changes".
- Phase 3 (팀장 관리): `/team/manage` (leaders/admin; tab shown conditionally in `team/layout.tsx`). Insights and attention rules live in `lib/team/insights.ts` — add new rules there, not in the page. Leader actions on a teammate go through `managerOf()` in `lib/plans/actions.ts` (admin or that team's leader, never self).
- Weekly team report: `team_reports` (one per team/week, draft|final). Generation = `collectWeekSource()` → `renderSource()` → `chatCompletion()` (OpenAI, fetch only, no SDK) → upsert. Permissions via `reportAccess()` (admin all; leader own team edit; member read final only). Never log the API key or the full prompt; errors are mapped to Korean messages in `openai.ts`. To test without a real key, point `OPENAI_BASE_URL` at the mock server in the scratchpad e2e folder.
- Leave requests: types/costs live in `lib/leaves/types.ts` (change 조퇴/반차 rules there, both form and actions read them). `createLeaveRequest` is the only way members add leave. It writes only the `leave_requests` row (status `submitted` = 승인 대기); `decideLeaveRequest()` approves (re-checks clashes/balance, refreshes the print snapshot, writes `leaves` rows with request_id) or rejects (reason required). So everything that reads `leaves` (calendars, timelines, balance, insights) shows approved leave only. Approver = admin or the requester's team leader, never self (`requestAccess().canApprove`). Every 품의 create action uses `requestAccess().autoApproves(memberId)`: a team leader's own documents and an admin filing on behalf of someone are approved at creation (leave then writes its `leaves` rows immediately). Pending days count against the balance at submit time (`pendingUsage()`), and overlapping pending requests are refused. Pending requests surface in the 오늘 approval inbox (`approvals.tsx`, shared with milestone proposals), the 일정 header badge and the 휴가 품의서 tab badge. `ApprovalActions` (components/approval-actions.tsx) takes a bound server action, e.g. `decideLeaveRequest.bind(null, id)`. The print page under `src/app/print/` is outside the `(app)` layout on purpose (no chrome) but still behind `requireUser`. Keep table `<colgroup>` widths in sync with the DOCX grid (dxa values in comments) if the company changes the form.
- zsh note: quote paths containing `[id]` in shell commands (glob) — an unquoted `mkdir src/app/x/[id]` silently fails.
- Members have both `position` (직책, required) and `rank` (직급, optional) plus `phone`. Printed 휴가 품의서 uses `rank || position` for the 직위 cell (snapshotted into `leave_requests.position`).
- Leave balance: rules in `lib/leaves/policy.ts` (pure, client-safe: leave year from join date, default annual by year of service with first-year monthly accrual), aggregation in `lib/leaves/balance.ts` (server). `members.annual_override` overrides the default; `total_offdays` is legacy and must not be read. Form preview, admin table and `createLeaveRequest` all go through `leaveBalance()`.
- AI documents: `components/ai-doc/ai-doc-editor.tsx` is the generic editor (pass bound server actions via inline `"use server"` closures from the page). Meeting notes (`lib/meetings/*`) reuse `collectWeekSource()` from reports for both last and this week plus `buildInsights()` flags. Keep the report's `report-editor.tsx` or migrate it to the generic editor when touching it next.
- Dashboard week timeline shows leaves only (user asked to keep the first page simple). Milestone views: Gantt + month calendar under `/team/milestones` (`?view=calendar`). `packLanes` from `components/week-timeline.tsx` is shared for bar packing.
- Login identifier is `users.email` (falls back to `users.username` for legacy rows). New member accounts: `username = email = members.email`, admin supplies only the initial password (`linkAccount` new mode). Editing a member's email updates the linked user's email. UI shows `user.email ?? @username`.
- Weekly member review (핵심 루프): `member_reviews` (one per member/week, draft|shared, rating 1–5 optional, summary/strengths/improvements/nextActions, member `ackAt` + `reply`). Leader UI `/team/reviews` (tab for leaders/admin), member UI `/my/feedback` + card on 오늘. Permissions reuse `reviewerContext()` (`canReview` for writes; members only touch their own *shared* review). AI draft = `collectMemberWeek()` → `renderMemberWeek()` → `chatCompletion(..., { json: true })` → `parseMemberReview()`; it returns fields to the form and never saves on its own. Sharing with "다음 주 항목으로 지정" inserts leader-assigned `weekly_items` for weekStart+7 (dedup by title). Default review week = `defaultReviewWeek()` (this week from its last working day, else last week). Nav badges (unread feedback / teammates left to review) are computed in `app-header.tsx`.
- Design system: brand magenta `--brand` (DIVERSE #961A7F) drives `primary`/`ring`/`accent`; use `bg-brand`, `bg-brand-soft`, `text-accent-foreground`, `bg-success`. Canvas is faint grey, cards white with a soft shadow (globals.css). `.bg-hero` is the gradient surface for page heroes. Motion via `motion/react`; shared pieces in `components/motion.tsx` (`FadeIn`, `ProgressBar`, `ProgressRing`, `EASE`); `MotionProvider` respects reduced motion. `(app)/template.tsx` fades pages in on area change. Keep animations short (≤0.4s) and functional.
- 오늘 page: one list (`today-goals.tsx`, optimistic via `useOptimistic`) for both planning and check-off; the old separate planner/review cards were removed on purpose. "퇴근 전 정리" = `daily_logs.done`.
- Milestone approval: any member can propose a milestone for their own team (`approval = pending`); leader/admin-created ones start `approved`. Leader of that team (or admin) approves/rejects via `decideMilestone()` (reject needs a reason, shown to the proposer); a proposer editing a rejected one resubmits it (→ pending). Rules live in `milestoneAccess()` (`canCreateFor`, `autoApproves`, `canApprove`, `canManage`, `approveTeamIds`). `milestonesInRange()` returns approved only unless `{ includeUnapproved: true }` (only the milestone page uses it), so pickers/insights/reports/AI never see proposals; `checkMilestone()` in plans actions also requires approved. Pending proposals surface on 오늘 (`PendingApprovals`), the 마일스톤 tab badge and the 팀 header badge.
- Date inputs: never use `<Input type="date">`. Use `DatePicker` / `DateRangePicker` from `components/date-picker.tsx` (shadcn Calendar + Popover, react-day-picker v10, `ko` locale). They take/emit `YYYY-MM-DD` keys and submit via hidden inputs (`name`, or `startName`/`endName`), so server actions read the same FormData fields. Use the range picker for any start/end pair (milestones, leave). `dropdown` adds year/month selects for far dates (입사일). Server-side validation stays the source of truth (hidden inputs don't do native `required`).
- 팀 → 구성원 (`/team/members`, leaders/admin): team roster (직급, 직책·직무 = `position`, 입사일/근속 via `tenure()`, 연차/병가 남은/총 from `leaveBalance()`, latest shared review) and `/team/members/[id]` detail (8-week record table from `collectMemberWeek().stats`, weekly reviews incl. drafts, leave requests with 작성일/승인일, owned milestones). Permissions: `memberAccess()` in `lib/members/access.ts` — leaders add/edit members of their own team only (no team moves, no `annualOverride`; server keeps the existing value) and may create a login (`linkAccount` mode "new", or the optional initial password on create). Delete / linking existing accounts / overrides stay admin-only. Member form + dialog live in `components/members/` (shared with 관리 → 구성원).
- Team isolation: non-admins only ever see their own team. Use `teamScopedMembers(user)` (lib/members/access.ts) for any team-wide list (팀 현황, 일정 달력, 오늘 휴가 타임라인, 업무대행 후보). Milestones page filters by `inScope(teamId)` (list, owner options, `?m=` detail). Server-side: milestone links require approved + same team as the member (`checkMilestone(id, memberId)`), milestone owner must be on the milestone's team. Reports/meeting notes (`reportAccess`), leave requests (`requestAccess.canView`), reviews (`reviewerContext`) were already team-scoped. Admin sees everything.
- 인사평가: `member_evaluations` (one per member per quarter "YYYY-Q1".."Q4"; `?period=YYYY` shows the yearly average computed by `yearSummary()` from finalized quarters only, never stored; future quarters are disabled and rejected server-side; `scores` JSON of criterion key → 1–10, `total` = average, draft|final). Criteria, grades (S≥9, A≥8, B≥6.5, C≥5, D) and period helpers live in `lib/evaluations/types.ts` — add criteria at the end, never rename keys. Edited on `/team/members/[id]` (`evaluation-form.tsx`, `?period=`), saved via `saveEvaluation()` (`reviewerContext().canReview`: admin or the member's leader, never self). Private HR data: members never see it; don't surface it on member-facing pages. `evaluationReference()` supplies period facts (record habits, completion, weekly review avg, leave/조퇴) shown beside the scores.
- 인사평가 AI 채점: `generateEvaluationDraft()` → `evaluationReference()` + `renderEvaluationSource()` (week-by-week digest of daily/weekly records, weekly reviews, leave; capped per week) → `chatCompletion(..., { json: true })` → `parseEvaluation()` (per criterion `{score, reason}`, clamped 1–10). Returns the draft to the form; never saves by itself. `member_evaluations.reasons` (criterion → 근거) is saved with the evaluation, plus `aiModel`/`aiGeneratedAt` when the saved version came from an AI draft. Stars are display only (`starsOf()`: 10점 = 5 stars).
- 품의 area (`/forms`, header menu after 일정): card grid of all 품의서 kinds + per-year counts of the user's own documents, and `/forms/mine` (unified 내 품의 내역, filter by year/kind). Registry = `FORMS` in `lib/forms/types.ts` (client-safe); icons in `components/forms/form-icon.tsx`. Unbuilt kinds open the `/forms/[kind]` "양식 준비 중" placeholder. To add a form: build its pages (e.g. `/forms/<kind>/new`, list, detail, print), set `ready: true` + `newHref`/`listHref`, and add its rows to `myFormCounts()` and `myDocuments()` in `lib/forms/queries.ts`. Counts exclude cancelled documents. 휴가 품의 stays at `/schedule/requests*`.
- 시간외 근무신청서 (first form built on the 품의 registry): `overtime_requests` (startAt/endAt "YYYY-MM-DDTHH:mm" KST wall time, hours, reason, member snapshot 부서/직위/담당업무/성명, same status + decision fields as leave). Pages `/forms/overtime` (list, team-scoped via `requestAccess().canView`), `/new`, `/[id]` (approval banner, cancel), print `/print/overtime/[id]` (mirrors `templates/overtime-request.docx`; 신청일/신청인 auto-filled, 부서장 = approver once approved). Actions in `lib/overtime/actions.ts` reuse `requestAccess()` for approve/cancel rules; overlapping pending/approved requests are refused; max 24h per request. Pending items show in the 오늘 inbox (`ApprovalInbox overtime`), "내 승인 요청", and the 품의 header badge. Use this module as the template for the remaining forms. `CancelRequestButton` (components/cancel-request-button.tsx) takes a bound cancel action.
- 일반 품의서: `general_requests` (제목, 목적, 거래처, 기간 `period` and 시기 `timing` as free text (no calendar, empty = omitted), `amount` in minor units of `currency` (KRW/USD/EUR/JPY/CNY/GBP; parse/format via `parseAmount`/`formatAmount`/`amountLine` in `lib/general/types.ts`) + `vat` included|excluded|none, 지급계좌, 추가 내용 `extra`, 첨부 text, 보존기간 `retention` years/0=영구) with the same status/decision fields and `requestAccess()` rules as leave/overtime. Pages `/forms/general` (list), `/new`, `/[id]`; print `/print/general/[id]` mirrors `templates/general-request.docx` (1차/2차 결재 each span 4 rows, 1차 has no 대표이사, 제목 row; approval fills the 1차 작성자 box with name + MM/DD; empty 기간/시기 lines drop out and the list renumbers). Constants in `lib/general/types.ts`. Wired into the 오늘 inbox/내 승인 요청 (`general` prop) and the 품의 header badge.
- 택시비 지급 품의서: `taxi_requests` (지급 요청 사유, 이용 기간 useStart/useEnd via DateRangePicker (≤ today), amount 원, 지급계좌, 첨부 text, retention). 제목 is fixed (`TAXI_TITLE`), 부서명/작성 담당자 come from the member snapshot. Pages `/forms/taxi`, `/new`, `/[id]`; print `/print/taxi/[id]` (templates/taxi-request.docx). Print 결재표 for 일반/택시비 is the shared `ApprovalTable` + `KindHeader` in `src/app/print/_components/approval-table.tsx` (`firstRowCeo` adds 대표이사 to the 1차 row, `receiverDept` pre-prints 수신 부서) — reuse it for the remaining 품의서 that share this grid. Lint: helpers must not start with `use` (react-hooks rule).
- 회식비 품의 pair (linked): `dinner_requests` with `stage` budget (① 회식비 전산품의, print templates/dinner-budget-request.docx = "회식비 청구 품의서") and settle (② 회식비 청구품의, print templates/dinner-settle-request.docx = "회식비 정산 품의서"); settle rows point at their budget via `parentId`. Fixed 제목 per stage (`DINNER_TITLE`), fixed 목적 `dinnerPurpose(team)`, fixed 1인당 한도 `DINNER_LIMIT_PER_PERSON` (amount ≤ first number in 인원 × limit, enforced on budget). Settle is created only from an approved budget of the same member (`createDinnerSettle(budgetId)`), copies 인원/한도/금액/결제 방식, adds 시기 (dinnerDate ≤ today) + 지급계좌; one live settle per budget (`liveSettleOf`), and a budget with a live settle can't be cancelled. Pages under `/forms/dinner` (list with 청구 대기 strip, `/new`, `/claim` picker, `/[id]`, `/[id]/settle`), print `/print/dinner/[id]` (shared `ApprovalTable`, `firstRowCeo`, 수신 경영지원실). Registry kinds: dinner_system = budget, dinner_claim = settle.
- Form visibility: `FormDef.leaderOnly` hides a form from non-leaders; pages use `formsFor(canUseDinner(user))`. 회식비 전산/청구품의 are leader-only (`canUseDinner()` in lib/dinner/access.ts: admin or team leader) — enforced by `/forms/dinner/layout.tsx` (redirects to /forms) and in both create actions; admin can only file them in a leader's name. 사직서 and 출장 품의 were removed from the registry by request (their kinds no longer exist; `/forms/resignation` 404s).
